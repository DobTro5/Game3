public class LevelManager {
    public void loadLevel(int level) {
        switch (level) {
            case 1:
                // Настройки для уровня 1 break;
            case 2:
                // Настройки для уровня 2 (например, снег)
                break;
            case 3:
                // Настройки для уровня 3 (например, дождь)
                break;
            // и так далее }
        }
    }
